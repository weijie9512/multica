# Multica Customizations

This file tracks the private, non-upstreamable customizations applied on top of `multica-ai/multica` in this fork. Each entry corresponds to one commit on the `customize` branch. When upstream refactors a file touched here, use this file to re-derive intent during conflict resolution.

Design doc: `../../PROJECT.md` §3 (Soft fork with upstream sync).

---

## 1. `working_dir` field on projects

**Commit subject:** `customize: add working_dir field to projects`
**Issue this solves:** Multica's daemon hardcodes the spawn directory to `{MULTICA_WORKSPACES_ROOT}/{workspace_id}/{task_id_short}/workdir/` (see `server/internal/daemon/execenv/execenv.go:68–94`). For a single-developer setup where projects map to real git repos on the host, I want the agent to spawn directly inside the repo so it can see my real files and real git state — not an isolated scratch directory.

**Scope:** Add a nullable `working_dir TEXT` column to the `project` table. When set, the daemon spawns the agent (Claude Code / Codex) with `cwd = project.working_dir` for every task attached to an issue in that project. When null, existing behavior is preserved (isolated directory under `MULTICA_WORKSPACES_ROOT`).

**Key implementation trick:** Instead of adding a new field to the daemon claim response and touching `execenv` internals, the change piggybacks on the existing `PriorWorkDir` mechanism. The claim handler (`server/internal/handler/daemon.go:ClaimTaskByRuntime`) hijacks `resp.PriorWorkDir` with `project.working_dir` when set, and the daemon's existing `execenv.Reuse()` code path (`server/internal/daemon/daemon.go:904–907`) picks up the override with zero daemon-internal changes. This cut the diff from ~10 files to ~8 hand-edited files.

**Priority rule:** `project.working_dir` always wins over the historical `prior_session.work_dir`. An explicit project config beats a stale reused path.

**Fallback behavior:**
- Issue has no `project_id` → default behavior (isolated under `WORKSPACES_ROOT`)
- Project has `working_dir = NULL` → default behavior
- `working_dir` is set but the path doesn't exist on the host → `execenv.Reuse()` returns nil and the daemon silently falls back to `Prepare()` which creates the isolated directory. No loud error. A future hardening pass could surface this as a task failure with a clear message.

**Files touched (10):**

| File | Change | Hand-edited? |
|---|---|---|
| `server/migrations/040_project_workdir.up.sql` | New — `ALTER TABLE project ADD COLUMN working_dir TEXT` | ✅ |
| `server/migrations/040_project_workdir.down.sql` | New — rollback | ✅ |
| `server/pkg/db/queries/project.sql` | Add `working_dir` to `CreateProject` and `UpdateProject` | ✅ |
| `server/pkg/db/generated/project.sql.go` | Regenerated by `make sqlc` | ⚙️ |
| `server/pkg/db/generated/models.go` | Regenerated — adds `WorkingDir` to `db.Project` struct | ⚙️ |
| `server/internal/handler/project.go` | Thread `WorkingDir` through request/response structs + CRUD handlers | ✅ |
| `server/internal/handler/daemon.go` | Hijack `resp.PriorWorkDir` from `project.working_dir` in `ClaimTaskByRuntime` | ✅ |
| `packages/core/types/project.ts` | Add `working_dir` to `Project`, `CreateProjectRequest`, `UpdateProjectRequest` | ✅ |
| `packages/views/projects/components/projects-page.tsx` | Add text input in the create dialog between description and property toolbar | ✅ |
| `packages/views/projects/components/project-detail.tsx` | Add "Working Directory" section below description with blur-to-commit input | ✅ |

**Hand-edited count: 8.** Generated files (`project.sql.go`, `models.go`) are mechanical artifacts of `make sqlc` and don't count against PROJECT.md's ~10-file discipline cap.

**Upstream conflict risk:**
- `server/pkg/db/queries/project.sql` — recurring conflicts likely when upstream modifies `CreateProject` or `UpdateProject`. `git rerere` should auto-replay the simple "add one column" resolution.
- `server/internal/handler/daemon.go:ClaimTaskByRuntime` — load-bearing for the daemon. Watch for upstream changes to task-claim flow.
- `server/internal/handler/project.go` — stable; only low-risk expansions expected upstream.
- `packages/views/projects/components/*.tsx` — medium risk; upstream iterates on the projects UI.

**Smoke test (Phase 1 sign-off, 2026-04-11):**
1. Created project "Phase 1 — workdir smoke test" with `working_dir=/tmp/multica-phase1-smoketest`
2. Created issue WEI-2 linked to the project
3. Assigned to the local Claude agent
4. Daemon log confirmed: `execenv: reusing env workdir=/tmp/multica-phase1-smoketest`, `starting agent workdir=/tmp/multica-phase1-smoketest reused=true`, `claude started pid=11079 cwd=/tmp/multica-phase1-smoketest`
5. The agent itself `pwd`'d inside the directory, confirmed `/private/tmp/multica-phase1-smoketest`, and marked the issue as done. Total runtime: 32s.

---

## 2. Wiki metadata fields on issues

**Commit subject:** `customize: add wiki metadata fields to issues`
**Issue this solves:** The Phase 5 sidecar needs a way to know, per-issue, whether to pre-fetch wiki context before agent spawn, whether to write a post-task summary back to the wiki, and optionally what query to use instead of the issue title. Without these fields, the sidecar would have to apply global rules to every issue, which defeats the point of selective knowledge injection.

**Scope:** Three new columns on the `issue` table:

| Column | Type | Default | Purpose |
|---|---|---|---|
| `consult_wiki` | `BOOLEAN NOT NULL` | `FALSE` | When true, the sidecar calls `llm-wiki /search` and posts the result as a comment on the issue before the agent starts work |
| `allow_wiki_writes` | `BOOLEAN NOT NULL` | `FALSE` | When true, the sidecar calls `llm-wiki /entries` with the agent's final summary on task completion |
| `wiki_query_hint` | `TEXT` | `NULL` | Optional free-form hint to bias the wiki search; falls back to the issue title when null or empty |

All defaults keep existing rows backward-compatible — the sidecar is opt-in per issue.

**Key implementation decisions:**

- **Real columns, not JSONB squatting.** Same rationale as customization #1: these are stable concepts, the schema delta is tiny (3 columns, additive), and the UI needs native types for checkboxes and text inputs. JSONB squatting was considered (PROJECT.md §3) and rejected.
- **No daemon changes.** The sidecar (Phase 5) reads these fields through Multica's public REST API, not through the daemon claim path. The daemon code in `server/internal/daemon/` is untouched — this customization only adds fields that the REST layer exposes.
- **Update handler uses `pgtype.Bool` for nullable-in-update.** The sqlc `UPDATE` query uses `COALESCE(sqlc.narg('consult_wiki'), consult_wiki)` so omitting the field in a PATCH preserves the previous value. Generated `UpdateIssueParams` types the bools as `pgtype.Bool` even though the column is `NOT NULL` — this is correct; the nullability there is "present-in-update" not "present-in-db".
- **Create handler defaults.** `CreateIssueRequest` treats the bools as `*bool` for optionality: missing → false, explicit false → false, explicit true → true. Matches the user intent that new issues opt-in to wiki behavior.

**Files touched (10):**

| File | Change | Hand-edited? |
|---|---|---|
| `server/migrations/041_issue_wiki_fields.up.sql` | New — `ALTER TABLE issue ADD COLUMN …` × 3 | ✅ |
| `server/migrations/041_issue_wiki_fields.down.sql` | New — rollback | ✅ |
| `server/pkg/db/queries/issue.sql` | Add 3 fields to `CreateIssue`, add 3 narg updates to `UpdateIssue` | ✅ |
| `server/pkg/db/generated/issue.sql.go` | Regenerated by `make sqlc` | ⚙️ |
| `server/pkg/db/generated/models.go` | Regenerated — adds `ConsultWiki`, `AllowWikiWrites`, `WikiQueryHint` to `db.Issue` | ⚙️ |
| `server/internal/handler/issue.go` | Thread fields through `IssueResponse`, `CreateIssueRequest`, `UpdateIssueRequest`, and both handlers | ✅ |
| `packages/core/types/issue.ts` | Add `consult_wiki`, `allow_wiki_writes`, `wiki_query_hint` to `Issue` interface | ✅ |
| `packages/core/types/api.ts` | Add same fields to `CreateIssueRequest` and `UpdateIssueRequest` | ✅ |
| `packages/views/modals/create-issue.tsx` | Add compact "wiki metadata strip" (2 checkboxes + 1 text input) between description and property toolbar | ✅ |
| `packages/views/issues/components/issue-detail.tsx` | Add two `PropRow`s inside the Properties panel: `Wiki` (two checkboxes) and `Hint` (blur-to-commit text input) | ✅ |

**Hand-edited count: 8.** Under PROJECT.md's ~10-file cap.

**Upstream conflict risk:**

- `server/pkg/db/queries/issue.sql` — high churn file upstream; any new column upstream adds to `CreateIssue` or `UpdateIssue` will require re-ordering the narg list around mine. `git rerere` should replay once resolved.
- `server/internal/handler/issue.go` — very active (1380+ lines), high conflict risk. My insertions cluster around the `CreateIssueRequest`, `UpdateIssueRequest`, and the update path's rawFields branching. Upstream occasionally refactors these same sections.
- `packages/views/modals/create-issue.tsx` — medium risk. Upstream iterates on the create-issue UX.
- `packages/views/issues/components/issue-detail.tsx` — medium risk. The Properties panel is stable-ish but large.
- `server/pkg/db/generated/*` — mechanical, just re-run `make sqlc` after conflict resolution.

**Smoke test (Phase 4 sign-off, 2026-04-11):**
1. Ran migration, confirmed columns exist on `issue` table with correct defaults
2. Created issue via REST API with `consult_wiki=true`, `allow_wiki_writes=true`, `wiki_query_hint="authentication middleware"` — round-trips cleanly
3. Updated same issue via PATCH, toggled `consult_wiki` to false, confirmed the other fields preserved
4. Created issue in the UI with the new wiki strip visible, checked both boxes, typed a hint, saved, confirmed fields persisted
5. Opened issue detail, toggled checkboxes inline and blur-committed a new hint, confirmed optimistic updates + DB round-trip

---

## Future customizations

When adding the next one, append a new numbered section above this line. One commit per feature on the `customize` branch per PROJECT.md §3. Hard cap: 10 files per feature.
